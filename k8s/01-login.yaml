# ─────────────────────────────────────────────
#  LOGIN APP — Deployment + Service
#  kubectl apply -f 01-login.yaml
#
#  NOTE: Replace <AWS_ACCOUNT_ID> and <REGION>
#        with your actual ECR values
#  ECR image format:
#  <AWS_ACCOUNT_ID>.dkr.ecr.<REGION>.amazonaws.com/login-app:v1
# ─────────────────────────────────────────────

apiVersion: apps/v1
kind: Deployment
metadata:
  name: login-deployment
  namespace: hiqode
  labels:
    app: login-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: login-app
  template:
    metadata:
      labels:
        app: login-app
    spec:
      containers:
        - name: login-app
          image: 865189140490.dkr.ecr.us-east-1.amazonaws.com/login-app:v3
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "100m"
              memory: "128Mi"

---

apiVersion: v1
kind: Service
metadata:
  name: login-service
  namespace: hiqode
  labels:
    app: login-app
spec:
  selector:
    app: login-app
  ports:
    - protocol: TCP
      port: 80          # Service port
      targetPort: 80    # Container port
  type: ClusterIP       # Internal only — Ingress will expose it
